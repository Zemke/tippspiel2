<div class="tooltip" data-tooltip="{{t (concat 'evaluation.tooltip.' tooltip)}}">
  <span class="icon badge" data-badge="{{evaluation}}">
    <i class="fa fa-{{evaluationIconClass}}"></i>
  </span>
</div>
